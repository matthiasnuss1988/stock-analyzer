[build]
  publish = "./"                          # Ort der index.html etc.
  functions = "netlify/functions"        # Ort deiner Python-Funktionen

[functions]
  node_bundler = "esbuild"               # notwendig, aber irrelevant für Python

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
